# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['simple_agent.py'],
    pathex=[],
    binaries=[],
    datas=[('skills.py', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='SimpleAgent',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['simpleagent_logo.icns'],
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='SimpleAgent',
)
app = BUNDLE(
    coll,
    name='SimpleAgent.app',
    icon='simpleagent_logo.icns',
    bundle_identifier=None,
)
