# Default SimpleAgent workflow

This is the default workflow for all general prompts.

prompt_start: "default"
add_persona_context
add_recent_messages
add_memory_context
add_attachment_context
add_web_context
add_original_user_prompt
prompt: "output"
prompt_end